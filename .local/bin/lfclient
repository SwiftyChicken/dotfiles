#!/usr/bin/env bash

## TO DO
# Find lf instance id
# output selected file

# Handle relative and absolute path's
dir=$([[ $1 == /* ]] && echo "$1" || echo "$PWD/$1")
lf -remote "send cd $dir"
toggle-lf.sh
